export enum ModalType {
  NONE = 'NONE',
  CONFIRM = 'CONFIRM',
  LOADING = 'LOADING',
  SUCCESS = 'SUCCESS',
  REVIEW = 'REVIEW',
  SETTINGS = 'SETTINGS',
  SELECT_TOKEN = 'SELECT_TOKEN',
  SELECT_ACCOUNT = 'SELECT_ACCOUNT',
  // Add new modal types here...
}
