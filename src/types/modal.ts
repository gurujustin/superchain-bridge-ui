export enum ModalType {
  NONE = 'NONE',
  CONFIRM = 'CONFIRM',
  LOADING = 'LOADING',
  SUCCESS = 'SUCCESS',
  REVIEW = 'REVIEW',
  SETTINGS = 'SETTINGS',
  SELECT_TOKEN = 'SELECT_TOKEN',
  // Add new modal types here...
}
